import React from 'react';
import {Link,useParams} from 'react-router-dom';
import {ArrowLeft,ArrowUpRight} from 'lucide-react';
import {projects} from '../data/projects.js';
import {profile} from '../data/profile.js';
export default function ProjectDetails(){const {slug}=useParams();const project=projects.find(p=>p.slug===slug);React.useEffect(()=>{window.scrollTo(0,0);document.title=project?`${project.title} — Manoj Kumar | Backend Project`:'Project not found — Manoj Kumar';document.querySelector('meta[name="description"]').content=project?.summary||'The requested project could not be found.';document.querySelector('meta[property="og:title"]').content=document.title;document.querySelector('meta[property="og:description"]').content=project?.summary||'Project not found.';const site=import.meta.env.VITE_SITE_URL?.replace(/\/$/,'');if(site){let link=document.querySelector('link[rel="canonical"]');if(!link){link=document.createElement('link');link.rel='canonical';document.head.append(link)}link.href=site+location.pathname;document.querySelector('meta[property="og:image"]').content=site+'/og-v2.png'}},[project]);if(!project)return <section className="not-found container"><span className="eyebrow">404 / PROJECT NOT FOUND</span><h1>No project<br/><em>at this route.</em></h1><p>Explore the available work instead.</p><Link className="button button-primary" to="/#work">Explore projects</Link></section>;const next=projects[(projects.indexOf(project)+1)%projects.length];return <div className="detail"><div className="container"><Link className="back-link" to="/#work"><ArrowLeft size={16}/> All projects</Link><div className="detail-hero"><div><span className="eyebrow">{project.scopeOnly?'ADDITIONAL WORK':'CASE STUDY'} / {project.category}</span><h1>{project.title}<span className="period">.</span></h1><p>{project.summary}</p><div className="detail-actions">{project.liveUrl&&<a className="button button-primary" href={project.liveUrl} target="_blank" rel="noreferrer">Visit live product <ArrowUpRight size={17}/></a>}<a className="button button-outline" href={`mailto:${profile.email}?subject=${encodeURIComponent(`Let's discuss ${project.title}`)}`}>Discuss similar work <ArrowUpRight size={17}/></a></div></div><div className="detail-glyph" aria-hidden="true">{project.title==='DOCTAR'?'✚':project.title==='DOCTAR AI'?'✳':project.title==='JOBROOMS'?'⌘':project.title==='SIXAPP'?'◈':project.title==='JOB PORTAL'?'▣':'↗'}</div></div><div className={project.scopeOnly?"detail-facts two":"detail-facts"}><div><span>STATUS</span><strong>{project.status}</strong></div><div><span>MY ROLE</span><strong>{project.role}</strong></div>{project.metric&&<div><span>ENGINEERING SIGNAL</span><strong>{project.metric}</strong></div>}</div><div className="detail-body"><div className="detail-intro"><span className="eyebrow">01 / {project.scopeOnly?'PROJECT SCOPE':'CONTRIBUTION'}</span><h2>{project.scopeOnly?<>Codebase <em>scope.</em></>:<>What I <em>built.</em></>}</h2>{project.scopeOnly&&<p className="scope-note">This project is part of my work history. The details below describe what is visible in the codebase; my individual contributions are not itemized in my resume.</p>}</div><ol className="contribution-list">{project.contributions.map((point,i)=><li key={point}><span>{String(i+1).padStart(2,'0')}</span><p>{point}</p></li>)}</ol></div><div className="detail-tech"><span className="eyebrow">02 / TECHNOLOGY</span><h2>{project.scopeOnly?'Technologies in codebase':'Built with'}</h2><div className="tag-row">{project.tech.map(t=><span key={t}>{t}</span>)}</div></div><div className="detail-next"><span className="eyebrow">NEXT PROJECT</span><Link to={`/projects/${next.slug}`}>{next.title} <ArrowUpRight size={31}/></Link></div></div></div>}


